<#
    Root module shim. The real code lives in the nested .psm1 files; this
    file exists so the .psd1 manifest has a RootModule to point at. We
    intentionally do nothing here -- the manifest's NestedModules list
    handles loading.
#>
